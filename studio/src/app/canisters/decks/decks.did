type DeckId = text;
type DeckBucketId = principal;
service : {
  entries: () -> (vec DeckBucketId) query;
  entriesAdmin: (principal) -> (vec DeckBucketId);
  get: (DeckId) -> (DeckBucketId) query;
  init: (DeckId) -> (DeckBucketId);
}
