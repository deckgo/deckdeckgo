type DeckId = text;
type DeckData = 
 record {
   header: opt text;
   name: text;
   owner_id: text;
 };
type Deck = 
 record {
   data: DeckData;
   id: DeckId;
 };
service : {
  del: (DeckId) -> (bool);
  get: (DeckId) -> (opt Deck);
  set: (Deck) -> ();
}
